[build]
  command = "npm run db:push && npm run db:seed && npm run build -w @finvault/shared && npm run build -w @finvault/web"
  publish = "apps/web/dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
